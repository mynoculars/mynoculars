# Design Decisions Log

Consolidated D-1…D-56. Sourced from code comments (`grep -rn "D-[0-9]" src/`) and
`design/Research_Agent_Design.md` §10 (which only ever logged D-27…D-30 as a
table — D-1…D-26 previously existed only scattered across module docstrings,
which is the gap this file closes). Populated as part of **P2-09**; previously
0 bytes despite being referenced by name in `internal/PHASE-2_PLAN.md` and
several source comments.

Numbers with no entry below (D-7, D-9, D-10, D-11) are referenced only as part
of a grouped citation in code (e.g. "D-3/D-4/D-9/D-13/D-17/D-22" for "graph
bounds") with no independent rationale text found in any file in this repo —
consult `design/Research_Agent_Design.md` for those, or treat their absence
here as a documentation gap in its own right.

| ID | Decision | Where it lives |
|---|---|---|
| **D-1** | `dispatch_tasks` never returns an empty `Send` list — an empty backlog always falls through to `compiler` instead of silently halting the graph. | `orchestration/graph.py` |
| **D-2** | Task dedup: `pending_tasks` is replace-on-write (no reducer — one producer per superstep); `completed_task_keys` is the dedup guard, not the backlog itself. | `state.py`, `agents/task_utils.py` |
| **D-3** | `iteration_depth` is the gather loop's only clock; ticked exactly once per cycle, in `progress_checker_node`. | `agents/gathering.py` |
| **D-4** | `recall_target` is the convergence bound the gather loop measures itself against. | `config.py`, `agents/gathering.py` |
| **D-5** | Every field a fanned-out worker writes must carry an `Annotated[...]` reducer; a reducerless field written by two parallel nodes in one superstep raises `InvalidUpdateError`. Made impossible by construction, not by testing. | `state.py` |
| **D-6** | Workers receive `WorkerPayload` (one `SearchTask`), never the full `ResearchState` — a worker cannot see other workers' tasks or unrelated state. | `state.py`, `orchestration/graph.py::dispatch_tasks` |
| **D-8** | Durable checkpointing: `PostgresSaver` when reachable, `MemorySaver` fallback otherwise, so a bare laptop still runs with zero infrastructure. `recursion_limit` is the invoke-time backstop. | `storage/postgres.py`, `config.py` |
| **D-12** | Telemetry only aggregates counters other nodes already recorded — it never invents a figure. (P2-07 extends this to provider-level counters without changing the principle.) | `agents/compilation.py::telemetry_node` |
| **D-13** | Cap-at-production: the task producer (not the dispatcher) ranks by priority and keeps only the top `max_fanout` — `graph.py`'s `dispatch_tasks` always sends everything it's given. | `agents/task_utils.py::cap_and_filter` |
| **D-14** | Convergence routing reads only `recall_score`/`iteration_depth`/`escalation_trigger` — never the backlog, which is stale by the time routing runs. | `orchestration/graph.py::route_convergence` |
| **D-15** | Worker return-key whitelist enforced at runtime (`WORKER_WRITABLE_KEYS`) — turns a rare concurrent `InvalidUpdateError` into a deterministic, first-run failure. | `orchestration/contracts.py` |
| **D-16** | Failed tasks are recorded separately from completed ones; a task that failed at depth *d* may be re-emitted only at depth > *d* — "failure is data," never silently retried in a tight loop or permanently burned. | `state.py`, `agents/gathering.py::search_worker`, `agents/task_utils.py` |
| **D-17** | Quality-gated coverage: a goal counts as covered only if it has evidence scoring **strictly above** `min_evidence_score` (P2-01 changed this from an inert `>= 0.0`). | `agents/gathering.py::progress_checker_node` |
| **D-18** | A goal with an unresolved contradiction is `contested` and cannot be `covered`, regardless of evidence volume — drives the gap generator toward adjudicating evidence. Detection machinery is wired; the semantic detector itself is P2-12 (not yet built — see Phase 2 backlog). | `agents/gathering.py::merger_node` |
| **D-19** | Telemetry counters must be monotonic countables only (call counts, flags) — never durations, which don't merge meaningfully across parallel workers. | `state.py::merge_counters` |
| **D-20** | Run identity is a `thread_id`; reusing an old one resumes/continues that checkpoint. | `cli.py`, `api/server.py` |
| **D-21** | Zero composed goals is a **legal** output, not an exception — routed to an explicit "planning failed" report (optionally via HITL's E1) rather than a `KeyError` or silent empty run. | `agents/planning.py::goal_manager_node` |
| **D-22** | Bounded critique loop: the critic's notes travel into the next compile pass (grounded rewrite, never a blind retry), bounded by `max_revisions`. | `agents/compilation.py::compiler_node`, `critic_node` |
| **D-23** | Human-in-the-loop escalation on four triggers (E1 zero goals, E2 contested non-convergence, E3 exhausted non-convergence, E4 critique exhaustion), off by default (`HITL_ENABLED=false`). P2-09 added disabled-mode `escalation.stub` logging for E2/E3, matching E1/E4's pre-existing parity. | `agents/escalation.py`, `agents/gathering.py`, `agents/planning.py`, `agents/compilation.py` |
| **D-24** | Cross-run semantic memory: retrieval reranked by similarity × volatility-aware decay (never a hard TTL); only evidence from a **passed** critique is written back. | `memory/semantic_memory.py` |
| **D-25** | Typed specialist workers: a `SearchTask.tool_hint` field (default `""`) lets a task-producing node route a task to a NAMED specialist worker instead of the default corpus search — today the only specialist is `"mcp"` (P2-13's tool), gated behind `settings.mcp_enabled`. `task_utils.py::cap_and_filter` is the only place a raw hint is ever validated against what's actually wired into the run; `orchestration/graph.py::dispatch_tasks` trusts it and just routes. Implemented, P2-14. | `state.py`, `agents/task_utils.py`, `orchestration/graph.py`, `cli.py` |
| **D-26** | Retrieval tools are mediated through a plain callable seam today (`ToolFn`) — the original design's MCP stand-in (D-30) is now the SAME seam, not a replacement of it: `tools/corpus_search.py` and `tools/mcp_client.py` are both `ToolFn` implementations, chosen (and, since P2-14, combined) at `cli.py`'s wiring layer. Implemented, P2-13. **Concurrency, fixed**: `scripts/mcp_corpus_server.py`'s tool handler is `async def` and offloads the blocking corpus lookup to a dedicated `ThreadPoolExecutor` (`mcp_max_workers`), instead of the original synchronous handler that blocked FastMCP's single event loop per call. A second, separate stall (the first-ever `qdrant_client` import happening lazily on a worker thread while the event loop was already doing real overlapped I/O, ~120s independent of any configured timeout) was found and fixed by importing `qdrant_client`/`opensearchpy` eagerly at module load. Confirmed live: 6 concurrent calls, 13.5s wall time vs 79.2s summed (ratio 1.02). See `README.md` Limitations #6 / `OPERATIONS.md` Troubleshooting for the full account. Off by default (`MCP_ENABLED=false`), never a correctness bug either way. | `tools/corpus_search.py`, `tools/mcp_client.py`, `scripts/mcp_corpus_server.py` |
| **D-27** | Memory retrieval fusion + decay runs server-side in Qdrant (`FormulaQuery` + `ExpDecayExpression`, mandatory payload indexes), gated by `settings.memory_server_side_decay` (off by default). The original Python path (`memory/semantic_memory.py::decay_factor`) is kept permanently as the parity oracle, never removed — see that module's own docstring. Implemented, P2-10. | `storage/qdrant_store.py`, `memory/semantic_memory.py` |
| **D-28** | Interrupt idempotency: an interrupting node **re-executes from its top** on resume — it does not resume mid-function. No non-idempotent effect may precede `interrupt()`; `escalation_history` is appended only in the resume-path update for exactly this reason. Resume taxonomy: approve / redirect / abort. | `agents/escalation.py` |
| **D-29** | `model_config = ConfigDict(extra="forbid")` on every state model — schema-level pollution defense at construction time, complementing D-15's runtime worker-return whitelist. Two layers, two distinct failure modes. | `state.py` |
| **D-30** | MCP transport policy (stdio implemented for local servers; Streamable HTTP for remote servers is a documented future variant, not yet built; SSE prohibited outright) plus tool-security invariants (explicit per-server env allowlist — `tools/mcp_client.py::_build_subprocess_env` never forwards `os.environ`, `AsyncExitStack` lifecycle for the stdio subprocess). Implemented (stdio), P2-13. | `tools/mcp_client.py` |
| **D-31** *(proposed, P2-03)* | Store writes should carry stable, content-derived identity rather than a fresh `uuid4()` per call — re-ingesting unchanged content should overwrite in place, not accumulate duplicates. Implemented for `QdrantStore.upsert_texts` via an optional `id_fn` parameter (default preserves old behaviour). Not yet added to `design/Research_Agent_Design.md` § 10's own decision table (this file, `DECISIONS.md`, is a SEPARATE, later consolidation -- see the preamble at the top of this file; D-31 IS formally logged here). | `storage/qdrant_store.py` |
| **D-32** *(proposed, P2-04)* | Provider output normalization happens at the client boundary (`llm/client.py`) — chat-template sentinels are stripped/truncated there, so nodes and the router never see transport or template artefacts. Covers both the JSON path (`_extract_json`) and the free-text path (`_truncate_at_sentinel`). Not yet added to `design/Research_Agent_Design.md` § 10's own decision table (this file, `DECISIONS.md`, is a SEPARATE, later consolidation -- see the preamble at the top of this file; D-32 IS formally logged here). | `llm/client.py` |
| **D-33** *(proposed)* | Live-service health checking (`scripts/check_services.py`) is a standalone, opt-in script, never a pytest test -- this repo's test suite is proudly, entirely offline (see `tests/conftest.py`'s module docstring), so a check depending on four-to-six live services has no place silently running (or silently skipping) inside that guarantee. PASS/FAIL/SKIP, not just PASS/FAIL: MCP_ENABLED=false (this repo's default) and a FastAPI server simply not running are both correct, unremarkable configurations, not failures -- conflating "not configured" with "broken" would make the tool cry wolf on every default install. The MCP check has no port or process to probe (D-30: stdio transport, no persistent server) -- it spawns a real, throwaway instance of the configured subprocess and makes one real tool call, the only meaningful liveness probe for that transport. | `scripts/check_services.py` |
| **D-34** *(proposed)* | The test suite is organized by SOURCE MODULE (`tests/unit/<module>.py`, one file per `src/research_agent/` module) plus `tests/integration/<scenario>.py` for full `graph.invoke()` runs -- not by the development phase/tier each test was originally written in. The prior five-file, phase-named layout (`test_core.py`/`test_tier2.py`/`test_tier3.py`/etc.) had `test_tier3.py` alone spanning at least six unrelated source modules by the time it reached 74 tests, defeating the "which file catches a regression in module X" question the layout should answer at a glance. | `tests/unit/`, `tests/integration/` |
| **D-35** *(Phase 3)* | Langfuse observability is isolated in one package, `research_agent/langfuse/`: `client.py` is the ONLY file that imports the `langfuse` SDK (`build_client()` returns a real client or `None`, never raises); every other business module (`cli.py`, `orchestration/graph.py`, `llm/router.py`, `llm/client.py`, `retrieval/hybrid.py`, `memory/semantic_memory.py`, `agents/gathering.py`, `agents/compilation.py`) imports only the thin public functions in `langfuse/__init__.py` (`start_trace`, `span`, `generation`, `event`, `score`, `end_trace`, `flush`, `shutdown`) and never sees an SDK object, a trace handle, or a span handle. Off by default (`LANGFUSE_ENABLED=false`): zero SDK import, zero network calls. A single generic `traced_node()` wrapper applied at `graph.py`'s 13 `add_node(...)` call sites spans every node without touching any node function body. The installed SDK is v4.x (OTel-based, no stateful `.trace()` object) — session/environment grouping goes through `propagate_attributes(...)`, verified live to survive both being read outside its lexical `with` block and LangGraph's own `contextvars`-based parallel dispatch. Nesting: `TraceContext` also accepts an optional `parent_span_id`, and the run's open root span was already tracked in `Observer._roots` for `end_trace`'s sake, so real OTel parentage needed no new state and no call-site changes; `_trace_context()` additionally yields to an already-current span of the same trace, so observations made inside a `span_ctx()` block nest under that block rather than under the root. (Earlier revisions of this row recorded flat traces as a deliberately deferred limitation — that is no longer accurate.) `Observer.span_ctx()` wraps the SDK's `start_as_current_observation` so a node span opens BEFORE the node runs and its timestamps are real; the post-hoc `span()` is kept for callers with no block to wrap, and carries `duration_ms` as metadata because a span created after the work finished would get a negative duration from a true end time. `api/server.py` opens and closes a root trace per REQUEST — not per run — because `propagate_attributes` is sync-only and attaches to the calling thread, and FastAPI serves these `def` endpoints from a reused threadpool; entering on one request and exiting on another would leak a session id onto the next request that worker handles. `create_score` takes `trace_id` ONLY: the ingestion API accepts exactly one score target and rejects `traceId`+`sessionId` together with HTTP 400, so scores inherit their session from the trace they attach to. | `langfuse/client.py`, `langfuse/observer.py`, `langfuse/helpers.py`, `langfuse/pricing.py`, `langfuse/__init__.py` |

| **D-36** | Application assembly (`AppBundle`, `build_app_and_settings`) lives in its own neutral module, `research_agent/assembly.py`, not in `cli.py`. `api/server.py` imports it from there. `cli.py` re-exports both names, so every pre-existing `from research_agent.cli import build_app_and_settings` call site keeps working unchanged. Rationale: `api/server.py` is a long-running HTTP service and was importing its entire startup path from a module named "cli" — merely odd while the API was a demonstration of the seam (D-33), actively wrong once the API is packaged and consumed by a separate project. Nothing about the assembly itself changed in the move; the function body is byte-identical. | `assembly.py`, `cli.py`, `api/server.py` |
| **D-37** | The repo is an installable artifact (`pyproject.toml`, setuptools backend, `src/` layout), not only a `PYTHONPATH=src` checkout. Optional dependency groups (`api`, `mcp`, `langfuse`, `viz`, `dev`, `all`) mirror exactly those code paths that are off by default AND lazily imported, so a core install genuinely omits FastAPI/uvicorn/MCP/Langfuse — verified in a clean venv, not assumed. A `research-agent` console script exposes the CLI. Versioning is manual SemVer in the `version` field, bumped with the change and tagged, deliberately NOT derived from git (a build-time dependency, and it hides the version from anyone reading the file). The PUBLIC surface a MAJOR bump is owed for is stated explicitly in that file's header comment: `assembly.build_app_and_settings`/`AppBundle`, the three HTTP request/response shapes, the console script's arguments, and the `.env` setting NAMES — everything else is internal. `requirements.txt` is unchanged and remains the development pin-set; `pyproject.toml` is what a consumer resolves against. | `pyproject.toml` |

| **D-38** | **Retrieval is a LADDER, not a single tier.** `tools/retrieval_chain.py` tries corpus -> ONE reformulated corpus retry -> MCP -> the answering model's own knowledge (`tools/model_knowledge.py`), stopping at the first tier returning evidence above `min_evidence_score` (the same predicate D-17 uses for coverage, passed in so the two can never drift). Rationale: before this the system had exactly one tier, and a corpus that did not contain the subject produced "the retrieved evidence does not cover it" -- reporting a RETRIEVAL limitation as an absence of knowledge, which is the wrong answer to give. Honesty invariants, all enforced: model-sourced items always carry `source="model"` and are never relabelled; `compile_report`'s old GROUNDING RULE (which forbade model knowledge outright, and was therefore the direct cause of the failure) is replaced by an ATTRIBUTION RULE requiring model-derived claims to be marked as general knowledge in the prose; `critique` is told not to fail a report for using attributed model knowledge; telemetry reports `corpus_recall` and `model_sourced_items` alongside `recall`, so "we answered it" and "the corpus answered it" can never be confused. The model tier is LAST, so a real document always wins. Off by one flag (`MODEL_KNOWLEDGE_ENABLED=false`) restores the exact pre-D-38 posture, escalation included. | `tools/retrieval_chain.py`, `tools/model_knowledge.py`, `prompts/templates.py`, `assembly.py`, `config.py` |
| **D-39** | **A tier "answered" only if its evidence is on TOPIC, not merely high-scoring.** `retrieval_chain._sufficient` now requires one shared distinctive term (4+ chars, minus query scaffolding) between the query and a hit that cleared `min_evidence_score`. Rationale: RRF score is an ORDINAL signal -- "best of what came back" -- and a fixed-k search over a small corpus always returns k documents, so there is no empty state and a score test alone can never fail. Live (run p205.90-check): asked for GDP per capita and demographics against a ten-document Redis corpus, both legs returned the same irrelevant documents, cross-leg agreement pushed them to ~1.0, tier 1 declared success, the model tier was never reached, and the report said the evidence did not cover it -- the exact give-up D-38 exists to remove, re-entering through the sufficiency test. One shared term is deliberately a floor against wholly off-topic matches, not a relevance ranker. | `tools/retrieval_chain.py` |
| **D-40** | **Citations are goal ids, never pasted evidence text and never internal scores.** `compile_report` specifies `[g1]` / `[g1][g3]` exactly, and forbids three things live output actually did: running the source sentence into the claim with no delimiter ("...whole blobRedis is an in-memory data store..."), printing internal bookkeeping (`[g1 \| model \| score=0.60]`) into user-facing prose, and inventing citation text for claims no evidence item supports (run p205.93-check attributed Cassandra and DynamoDB descriptions to a corpus containing neither). | `prompts/templates.py` |
| **D-41** | **The model tier gets hard anti-fabrication limits, because self-reported confidence does not catch confabulation.** `templates.model_knowledge` forbids inventing names of operations/exercises/programmes, tells the model to prefer stable facts over date-pinned ones, and requires confidence to reflect the WEAKEST part of a compound claim. Rationale: run p205.91-check emitted "the 2023 Balakot-style airstrikes" (Balakot was 2019) and several non-existent named initiatives, all above the 0.5 confidence gate -- models are most confident exactly when confabulating plausible specifics. This REDUCES the failure rate; it does not eliminate it, which is why `corpus_recall` and `model_sourced_items` are in telemetry and why the report must attribute model claims (D-38). | `prompts/templates.py` |

| **D-42** | **Model recollection never enters durable memory, and recalled memory may never re-frame a query.** `SemanticMemory.store_run` filters out `source="model"` as well as `source="memory"`; `templates.compose_goals` labels recalled facts as background from UNRELATED runs and forbids them narrowing the question. Rationale, from runs p205.96/.97-check: the army run wrote 28 memory items of which 24 were model recollection, and the next, unrelated run ("Compare India and US" — an open question that had previously produced economic and demographic goals) recalled five of them and composed an entirely MILITARY goal set, inheriting PLA-doctrine prose verbatim into an India-vs-US report. Two independent reasons the write must not happen: storing recollection caches a lookup that was never expensive, and it launders an unverified guess into something a later run reads back tagged `source="memory"`, indistinguishable from document-backed evidence, before any retrieval has run. Memory is for what RETRIEVAL found (D-24); recollection is not. | `memory/semantic_memory.py`, `prompts/templates.py` |
| **D-43** | **Citation correctness is enforced deterministically where it can be, and asked of the critic where it cannot.** `compilation._clean_citations` strips any verbatim run of an evidence item's own content from the prose (run p205.95-check pasted source sentences into claims) and drops `[gN]` markers naming goals that retrieved nothing, counting both into telemetry as `citations_pasted_evidence_removed` / `citations_to_unevidenced_goals`. What code cannot do is judge whether an EVIDENCED goal's evidence supports a given sentence — run p205.98-check cited `[g5]` for Netflix/Cassandra petabyte figures while g5's evidence was Redis session-caching text, with `model_sourced_items: 0` — so `templates.critique` now explicitly requires failing a report whose named entities, figures or dates appear in no evidence item of any source, and checking markers rather than trusting them. Deterministic half and semantic half, stated separately because they have different reliability. Also in this decision: `corpus_recall` applies the D-39 topical gate (it reported 1.0 against a Redis corpus for army and India-vs-US queries, fooled exactly as the ladder's old sufficiency test was); `complete_json` retries every sentinel segment rather than accepting complete()'s prose-correct first-segment choice (raw_chars 2895 → kept_chars 21 was costing a fallback hop on every structured call); and `generate_gaps` must spread tasks across uncovered goals (one cycle tagged all six tasks g1, the next all six g5). | `agents/compilation.py`, `prompts/templates.py`, `llm/client.py` |
| **D-44** | **The topical gate scales with query specificity, and an E4 redirect reaches retrieval.** (a) `retrieval_chain._sufficient` now requires `min(3, max(1, len(terms)//4))` shared distinctive terms, not one. Live (run p205.101-check): a nine-term query naming Redis, Cassandra, DynamoDB and petabyte scale matched a Redis session-caching document on the single word "redis", which stopped the ladder at tier 1, left `model_sourced_items` at 0, and produced a report saying "no retrieved evidence quantifies..." for all five goals — the give-up re-entering through a too-permissive gate. A long, specific query matching only on its one broad subject word is not a topical match. (b) E4 redirect routes to `gap_generator` with `human_guidance` set, not straight back to the compiler. Live (run p205.103-check): guidance asking for "inputs from global watchdogs, UN reports of press freedom, human rights abuses, democracy index" is a request for NEW EVIDENCE; recompiling the same evidence block cannot serve it, so revision 3 failed on identical missing support and re-raised E4. `critique_notes` still carries the guidance so the next compile sees it. Bounded by `max_escalations` as before. | `tools/retrieval_chain.py`, `agents/escalation.py` |
| **D-45** | **Deterministic citation repair only strips GLUED pastes, and prompts carry no worked examples.** (a) `_clean_citations` removed every verbatim occurrence of an evidence item's content; on an in-corpus query the compiler legitimately states corpus sentences almost word for word, so run p205.107-check -- retrieval perfect at `corpus_recall` 1.0 with 36 corpus items -- shipped six EMPTY report sections ("### Scalability", "### Security", "### Common Use Cases"...). The defect D-43 targeted was never quoting, it was the missing delimiter: the source sentence running into the claim with no boundary. That signature is exactly detectable as a preceding alphanumeric character, so only those are stripped now. (b) Five prompts carried concrete worked examples of past failures. The critique prompt's example -- a report citing `[g5]` for Netflix/Cassandra petabyte figures -- was echoed by the critic as a finding during a Redis vs Memcached run, and the compiler then wrote a section denying it. Rationale for a rule belongs in the code comment beside it, never in the prompt text: anything in a prompt can surface in the output. All five are now abstract, with the live evidence kept in comments. | `agents/compilation.py`, `prompts/templates.py` |

| **D-46** | **The critic is shown the evidence it is asked to verify against.** `templates.critique` took only the query, report and goals — yet D-43 instructs it to fail any named entity, figure or date that appears in no evidence item. That check was unanswerable: the critic had to guess. Live (runs p205.111/.112-check): both off-corpus reports were failed with "not supported by any evidence item" for figures the model tier HAD supplied (`model_sourced_items` 24 and 20) but which the critic never saw, costing two revisions and an E4 escalation on every off-corpus run. `critique()` now receives `state.evidence` and renders it source-tagged inside an `<evidence>` block, with an explicit reminder that items tagged `model` are evidence too. Bounded by the same 60-item tail the compiler uses. | `prompts/templates.py`, `agents/compilation.py` |
| **D-47** | **Convergence requires topical grounding, not just a score above the floor — reusing the exact gate `corpus_recall` already applies, not a second, weaker one.** `progress_checker_node` computes `grounded_score`: a covered goal only counts if at least one `source in ("corpus","mcp")` item also shares distinctive vocabulary with the goal's own description (`tools/retrieval_chain._distinctive_terms`). `route_convergence` will not accept `recall_score` reaching target as full convergence unless `grounded_score` also clears `grounded_recall_target` (0.5). The FIRST version of this check tested only source+score, no topic, and was fooled the same way `corpus_recall` itself was once fooled (D-39): a `gap_generator` task drifted onto off-topic corpus content that scored above the floor and, without the topical check, counted as grounding for an unrelated goal. Reusing `_distinctive_terms` instead of writing a second definition of "on topic" closes that gap by construction rather than by a second heuristic. This gate operates at CONVERGENCE time, judging evidence already collected — it does not stop off-topic evidence from being retrieved in the first place; see D-55 for the retrieval-time entry point this one didn't cover. | `agents/gathering.py`, `orchestration/graph.py::route_convergence`, `config.py` |
| **D-48** | **A starved retrieval floor is a run-level telemetry fact, not a routing decision.** `HybridRetriever` counts `retrieval_dense_candidates`/`retrieval_dropped_by_floor`; `telemetry_node` computes the ratio and WARNs (`retrieval.floor_starvation`) once it clears `retrieval_floor_warn_ratio` (0.8). Deliberately observational: a single dropped query is the floor doing its job, but a run where nearly every dense candidate is dropped means `MIN_SIMILARITY` is starving the leg outright — invisible before this except in raw per-query debug lines. | `retrieval/hybrid.py`, `agents/compilation.py::telemetry_node`, `config.py` |
| **D-49** | **A model-tier claim pairing a specific year with a specific quantity is flagged, not trusted, because self-reported confidence does not catch false precision.** `tools/model_knowledge.overspecific_span` is a deterministic regex check (no LLM call) requiring BOTH a year and a recognized quantity unit — percentages, energy/mass/area, and (added after a later run surfaced air-quality figures) µg/m³-class concentration units — in the same claim; a bare year or bare figure alone is common and not flagged. Sets `Evidence.hedge_specific`. Deliberately narrow rather than a catch-all "any number near any date" pattern, which would flag ordinary counts that carry no false-precision risk. | `tools/model_knowledge.py` |
| **D-50** | **A `gap_generator` task naming a goal id that does not exist in the current run is rejected before it is ever searched, not after.** `cap_and_filter` now checks a task's `goal_id` against the actual `state.goals` set; a well-formed task naming an unknown goal is dropped and folded into the existing `producer_rejects` counter, same as a malformed task (P2-06). Found live: `gap_generator` emitted a task tagged with a goal id `goal_manager` never produced, and it was retrieved, scored, and merged into evidence anyway — permanently uncoverable, since no `Goal` object with that id existed for `progress_checker` to ever mark covered. Empty `state.goals` skips the check rather than rejecting everything, since every real caller that could run with zero goals already routed elsewhere before reaching here (D-21). | `agents/task_utils.py` |
| **D-51** | **Detection and enforcement of a false-precision flag are two separate guardrails, because the compiler following a prompt instruction to hedge is not reliable enough on its own.** D-49 flags the claim on the evidence; `guardrails/hedging.enforce_hedging` (new package member, called from `compiler_node` immediately after the existing `clean_citations` pass) is the enforcement half — it searches the COMPILED REPORT for the same literal quantity span and, if it survived unhedged, appends a visible `(unverified figure)` marker after every occurrence, skipping any occurrence already preceded by an honest hedge word. Found live: `hedge_specific_items` reached 29 on one run with zero visible hedging anywhere in the shipped report — the prompt instruction alone was not sufficient. Does not remove or "correct" the number, only flags it in place, since guessing at a different number would be worse than an honest unverified marker. | `guardrails/hedging.py`, `agents/compilation.py::compiler_node` |
| **D-52** | **The API accepts a bounded query length, matching the `Field(...)` constraint style every other tunable in `Settings` already uses.** `ResearchRequest.query` gets `Field(min_length=1, max_length=2000)` — confirmed absent by reading the class directly; no length constraint existed anywhere between the wire and the graph. Rejected at the pydantic/FastAPI layer (422) before `_graph.invoke` is ever called, so an oversized or empty query never reaches `classify_node`. | `api/server.py` |
| **D-53** | **A quality judge that fails on every attempt in a run is a run-level telemetry fact, same shape as D-48.** `telemetry_node` computes `llm_quality_failure_ratio` and WARNs (`quality.judge_unreliable`) once it clears `quality_judge_warn_ratio` (0.5). `evaluation/quality.score_answer`'s fail-open design (a broken judge must never reject a good answer) is correct and unchanged by this — every live run to date has shown `llm_quality_calls_failed == llm_quality_calls` (2/2, 3/3), which nothing previously distinguished from a judge that scored everything 1.0 on merit. | `agents/compilation.py::telemetry_node`, `config.py` |
| **D-54** | **A generation budget is sent to every LLM provider, and a high total call count is observed, not enforced — because `max_depth`/`max_revisions`/`max_escalations`/`recursion_limit` already bound a run's worst case together, and no run has come near a runaway shape.** `llm_max_tokens` (4096, threaded through `OpenAICompatibleClient` and omitted from the request payload entirely when unset, so a caller that doesn't pass it gets a byte-identical pre-change request) bounds generation at the request level, complementing rather than replacing `_truncate_at_sentinel`'s existing after-the-fact cleanup. Separately, `run_call_budget_warn` (40) WARNs (`run.call_budget_high`, carrying `revision_cycles` and `len(escalation_history)` as context) once `llm_provider_calls` clears it. Deliberately NOT a circuit breaker: building one would be enforcing against a failure mode with no supporting evidence — the highest `llm_provider_calls` observed across live testing is 18, and the existing bounds already make the unbounded-redirect-loop scenario a breaker would defend against structurally impossible (a redirect requires a fresh human decision each time, and `max_escalations` caps the total). | `llm/client.py`, `llm/router.py`, `agents/compilation.py::telemetry_node`, `config.py` |
| **D-55** | **The topical gate D-44 added still had a floor low enough to defeat its own purpose on the tier most likely to need it.** `retrieval_chain._sufficient`'s scaling formula (`need = min(3, max(1, len(terms)//4))`) floored `need` at 1 for any query with <=7 distinctive terms -- which is EVERY `corpus_reformulated` retry by construction, since `_reformulate` caps its output at 6 words. Live (run p205.141-check): the reformulated query "Indian Army size composition Chinese PLA" (4 distinctive terms, need=1) matched a Memcached slab-allocator document on the single accidental word "size" (almost certainly from "chunk size classes"), which cleared the similarity floor at 0.57 and was merged into evidence under a real, correctly-tagged goal id -- no orphaning (D-50), no mislabeling, just a legitimate citation to a topically irrelevant document. That evidence then sat in `gap_generator`'s next-cycle prompt tail (`evidence[-10:]`, unfiltered by goal) and primed it toward more off-topic queries -- the exact mechanism behind the recurring `gap_generator` drift D-47/D-50 were built to contain but never explained. Fixed by raising the floor from `max(1, ...)` to `max(2, ...)`, with a `min(3, len(terms), max(2, len(terms)//4))` cap: `need` may never exceed the query's own distinctive-term count, since a 1-term query (e.g. a stub-generated "key differences") cannot possibly share 2 terms with anything, even a document constructed to be maximally relevant -- an existing integration test (`test_a_real_document_still_beats_recollection`) caught this exact edge case failing before the cap was added. **What this does not fix, confirmed live on the very next run (p205.145-check) after shipping**: `gap_generator` can still GENERATE an off-topic query outright (two genuine Redis/Memcached tasks under an army-comparison goal at depth 2) -- the fix closes one entry point (an accidental single-word match smuggling noise in as signal), not the generator's tendency to propose the wrong query in the first place, which remains open. That run's drift cost real compute but never reached the report, since the queries were honestly about Redis and the compiler's citation discipline had no reason to cite them in an army report. | `tools/retrieval_chain.py` |
| **D-56** | **A code review of D-51 and D-55 found three defects the tests that shipped with them did not cover; all three were fixable without changing either decision.** (a) `guardrails/hedging.enforce_hedging` matched a flagged span by plain substring, so the flagged figure "5.2%" matched INSIDE "15.2%" in a report reading "Growth was 15.2% ... not 5.2%", putting an `(unverified figure)` marker on a different, never-flagged number. A marker on a figure the detector never flagged is worse than a missing one -- it asserts something false about a number that may be perfectly well-sourced. Fixed with a preceding-character check (`_starts_inside_a_larger_number`); the genuine occurrence is still marked. (b) `_distinctive_terms` kept only tokens longer than three characters, which threw away exactly the tokens carrying the most topical signal in this project's real traffic: "GDP growth India US 2020-2023" retained neither GDP nor US, and an Indian-Army-vs-PLA query dropped PLA, its single most distinctive word. Short ALL-CAPS tokens are now kept -- an acronym is the opposite of filler, and length was the wrong proxy for it. (c) The same function counted bare numbers as distinctive terms, and numbers travel in PAIRS: "2020-2023" contributed two terms, so any off-topic document mentioning the same two years cleared D-55's two-term bar on years alone -- defeating that decision for precisely the date-ranged queries this project issues constantly. All-digit tokens are now dropped; mixed alphanumerics (pm10, 155mm) are real terms and are kept. Note (b) and (c) both change a function `corpus_recall` deliberately shares (D-43), so telemetry and the ladder gate move together, as intended. **D-55's claim to have closed the accidental-match entry point was too broad as written**: it closed the single-word case; the paired-year case survived it and is what (c) closes. | `guardrails/hedging.py`, `tools/retrieval_chain.py` |
| **D-57** | **Web search is a real retrieval tier, reached over its own MCP server subprocess, and its evidence covers goals without ever grounding them.** Phase 4. The gap: `corpus_search`, the corpus MCP server and semantic memory all resolve to the SAME ingested documents, so "the corpus does not contain it" fell straight through D-38's ladder to the model's own recollection — there was no tier that could return something the corpus never held. Five parts, each deliberate. **(a) Process isolation, not a library call.** `research_agent/websearch/` (provider protocol, DDGS provider, rank scoring, dedupe/domain-cap filtering) is imported ONLY by `scripts/mcp_web_search_server.py`, a second stdio MCP server. The agent process never imports `ddgs`, never issues an outbound request of its own, and never depends on which engine is configured — swapping DDGS for a keyed API is one new module plus one setting, touching nothing in `agents/`, `orchestration/` or `tools/`. Verified by `test_importing_the_package_does_not_require_ddgs`. **(b) `source="web"`, never `"mcp"`.** `make_web_search_tool` is a SEPARATE factory from `make_mcp_tool`, which is left byte-identical. This is the load-bearing correctness decision: `progress_checker_node` and `telemetry_node` both test `source in ("corpus","mcp")` as a proxy for "a real DOCUMENT backed this", so exposing web through the existing client unchanged would have made every snippet inflate `grounded_score` (G2/D-47) and `corpus_recall` (D-43), silently restoring the recall=1.0 / corpus_recall=0.0 blindness both metrics exist to expose. Web evidence COVERS a goal and never GROUNDS one — a run answered wholly from the web reads `recall 1.0 / grounded_score 0.0`, which is visible rather than flattering. Locked by regression tests in `test_agents_gathering.py` and `test_agents_compilation.py`. **(c) Rank-derived scores over a bounded band, computed server-side.** `websearch/scoring.rank_to_score` maps 1-based rank linearly onto `[WEB_SEARCH_MIN_SCORE, WEB_SEARCH_MAX_SCORE]` (0.60–0.75). A flat constant — the obvious alternative — would tell every consumer that result #1 and result #5 are equal evidence, discarding the only relevance signal a metasearch endpoint gives. The floor must exceed `min_evidence_score` or the tier is inert (D-17's predicate is a strict `>`), and the ceiling must stay below the ~1.0 a two-leg corpus hit reaches under RRF or D-38's ordering inverts; `config.warn_on_web_search_band` WARNs on both misconfigurations. Scoring runs server-side, and AFTER dedupe and the per-domain cap, so the band interpolates across the results actually returned. An item arriving with no usable score is DROPPED and counted, never defaulted — no default is safe, and the hardcoded `1.0` this codebase already shipped once in `make_mcp_tool` is the precedent. **(d) Tier 4, after `mcp`, before `model`.** In every deployment of this repo the tier-3 MCP server is `mcp_corpus_server.py`, so tiers 1–3 all resolve to the corpus; web is the first tier that can return something new, and a live snippet is better provenance than recollection. Off by default (`WEB_SEARCH_ENABLED=false`) — with it false the ladder is byte-identical to every pre-Phase-4 run. **(e) Web evidence never enters durable memory.** `store_run` excludes `source="web"` alongside `"memory"`/`"model"`, for D-42's reason plus one more: a snippet is volatile by construction, so a cached copy of today's result is a wrong answer next month with nothing marking it stale. A live lookup should be repeated, not remembered. Also in this decision: a SECOND `MCPBridge` and a separate `WEB_MCP_*` config namespace (the existing `MCP_*` block describes exactly one server and cannot describe two), closed by name in both `cli.py` and `api/server.py`; `WEB_MCP_SERVER_ENV_ALLOWLIST` defaulting NON-empty to `HTTPS_PROXY,HTTP_PROXY,NO_PROXY` because this is the only subprocess making outbound internet calls and `_build_subprocess_env` forwards nothing (D-30) — behind a proxy an empty allowlist fails every search as an opaque timeout; `Evidence.url`/`Evidence.domain` carried for a future deterministic Sources pass, D-40's `[gN]`-only prose rule left untouched; and `mcp>=1.9` pinned to `<2`, a Phase 1–3 regression found while establishing the baseline — mcp 2.0.0 moved `mcp.server.fastmcp` and breaks `mcp_corpus_server.py` plus 9 tests on a fresh install. | `research_agent/websearch/`, `scripts/mcp_web_search_server.py`, `tools/mcp_client.py`, `tools/retrieval_chain.py`, `assembly.py`, `state.py`, `config.py`, `memory/semantic_memory.py`, `cli.py`, `api/server.py`, `scripts/check_services.py` |
| **D-58** | **MCP server paths resolve against the repository root, and an empty server command means `sys.executable`.** `tools/mcp_client.py::MCPBridge` builds an `mcp.StdioServerParameters` without setting its `cwd` field, so every server subprocess inherits the CWD of whatever launched the agent — and any relative path in `MCP_SERVER_ARGS` or `WEB_MCP_SERVER_*` is resolved against THAT directory. Verified by spawning real subprocesses rather than assumed: `args=["scripts/x.py"]` with `cwd=<repo>` works and with `cwd=/tmp` fails as `McpError: Connection closed`; a relative command behaves the same way with `FileNotFoundError`. The shipped `MCP_SERVER_ARGS=scripts\mcp_corpus_server.py` has therefore always worked only because every documented invocation happens to start in the repo root — it breaks under a service manager, scheduled task, or IDE that sets its own working directory, and breaks OPAQUELY. `config.resolve_repo_path` now resolves relative values against `REPO_ROOT` (derived from `config.py`'s own `__file__`, never the CWD), normalizing backslashes first so one `.env` is portable across Windows and POSIX, and returning the original string when the resolved path does not exist — that existence check is what keeps a bare PATH name (`python3`, `uvx`, `npx`) working instead of becoming a guaranteed `FileNotFoundError`. Applied in `assembly.py` to BOTH bridges, so this also fixes the pre-existing corpus case; absolute paths and bare names pass through unchanged, so no working configuration changes meaning. Separately, `config.resolve_server_command` treats an EMPTY command as `sys.executable` and this is the RECOMMENDED configuration for `WEB_MCP_SERVER_COMMAND`, not a fallback: it is correct on every machine with zero configuration, keeps machine-specific absolute paths out of the committed `.env.example`, and guarantees the server runs in the SAME virtualenv as the agent. That last point is concrete rather than tidy — the search server imports `ddgs`, and a wrong interpreter kills the subprocess before the MCP handshake completes, surfacing as an opaque "Connection closed" rather than a readable `ImportError`. `check_services.py::check_web_search` drops its "command is empty" failure accordingly and resolves through the same two helpers, so the check exercises the real launch path rather than a lookalike. | `config.py`, `assembly.py`, `scripts/check_services.py`, `.env.example` |
| **D-59** | **The gap generator is told what the run is about, which goals this cycle is for, and when there is no cycle to run — closing the last documented cause of off-topic drift, which D-47/D-50/D-55/D-56 had each contained a symptom of without explaining.** Live (run p205.203-check), the `gaps` prompt was: a goal list, an evidence tail, and nothing else. Two independent defects fell out of that. **(a) The original question was never in the prompt.** `templates.generate_gaps` had no `query` parameter, so with the tail dominated by off-topic Redis corpus hits under an India-vs-US query, the generator returned six consecutive Redis/Memcached queries. This is worth stating precisely, because every prior decision in this range implicitly blamed the model: it was not free-associating and not ignoring an instruction — it was reading the only subject the prompt still showed it, and answering correctly. The question now leads the prompt, ahead of the tail, with an explicit clause marking the tail as retrieval output rather than a description of the topic. **(b) `gap_generator_node` never handled an empty `uncovered` list.** D-47's grounded gate routes here with recall already at target and every goal `covered` but ungrounded; the prompt then rendered "Uncovered goals: (none)" while still demanding queries for them — an unanswerable instruction that left the tail as the only usable signal, which is defect (a)'s amplifier. The node now targets the *ungrounded* goals in that case (reusing `has_grounded_evidence`, extracted to module level so `progress_checker_node` and this caller cannot drift apart the way `grounded_score` and `corpus_recall` once did), and returns an empty backlog WITHOUT an LLM call when neither set is non-empty — D-1's own terminal exit, and the correct move for a genuinely converged run. A human redirect always bypasses the skip, per the same reasoning D-38's guard already uses: guidance is information the evidence never had. **Also in this decision, two separate honesty fixes found in the same trace.** `guardrails/sources.append_web_sources` filtered listed sources by CITED GOAL only, never by topic — so nine Redis-monitoring URLs were attributed under `[g1]` in an India-vs-US report. The prose was clean (`clean_citations` and the compiler both held); the Sources section asserted support that did not exist, which is exactly the failure D-51 exists to prevent, one layer down. The same `_distinctive_terms` gate now applies, skipped when goals are not supplied or a goal yields no distinctive terms (an untestable claim is left alone, per `_sufficient`'s own posture). And `telemetry_node`'s `web_sources_listed`/`web_sources_suppressed` were read from `state.counters`, which `merge_counters` accumulates ADDITIVELY while `compiler_node` runs once per revision — so a two-rewrite run reported the sum of three compile attempts as a property of one report: 44 listed / 25 suppressed against 35 web items, for a report containing 34 entries. Every figure arithmetically correct, none describing the artifact the reader received. Both are now derived from `state.final_report` via a new pure `count_listed_sources`, which keeps D-12 intact — telemetry still only counts what happened, it just counts the right thing. | `prompts/templates.py`, `agents/gathering.py`, `guardrails/sources.py`, `agents/compilation.py` |
| **D-60** | **The free-text quality gate was measuring answer LENGTH, and the router's "last answer wins" policy made a provider outage the only thing preventing a bad report from shipping.** Three defects in one causal chain, diagnosed by diffing runs p205.211 (bad) and p205.212 (good) — same code, same query, and the ONLY difference was that Gemini errored in one and succeeded in the other. **(a) `evaluation/quality.score_answer` handed the judge `answer[:4000]`, a raw character slice.** A compiled report routinely runs 7,000–11,000 characters, so the judge was shown a report that stopped dead mid-word (`"...with the central gove"`) and correctly scored it incomplete: 0.45 for a 10,103-char report, 0.35 for a 4,670-char one, both against a 0.6 threshold, on consecutive runs. The gate was not measuring answer quality — it was measuring how far past 4,000 characters the answer ran, i.e. penalising the compiler for thoroughness. `_excerpt_for_judging` raises the cap to 16,000, cuts on a paragraph/line/sentence boundary (only one landing past 75% of the cap, so a break-free document cannot collapse to a stub), and appends an explicit marker telling the judge it is an excerpt. **(b) `llm/client.py` never read `finish_reason`.** A generation the PROVIDER itself reported as cut off was returned as a finished answer: gemini-3.5-flash produced 162 completion tokens ending mid-number and 160 ending mid-sentence, and both shipped as the final report. `complete()` now raises `TruncatedGenerationError` on `finish_reason == "length"` — but ONLY when `_truncate_at_sentinel` trimmed nothing, since a trimmed runaway reached its own end-of-turn and the `length` describes the discarded tail, not the answer. Raising is the correct layer: this class's contract already reads "raise upward, the router owns fallback policy" (D-32). **(c) `FallbackRouter.complete` returned the LAST answer, not the BEST.** A rejected answer was discarded permanently, and the last provider in the chain is exempt from the quality gate — so p205.211 shipped a 732-char fragment (3 sections, 1 goal cited; then 652 chars with ZERO citations on the revision) over a complete report, while p205.212 shipped the correct 11,121-char report only because Gemini raised `HTTPStatusError` and `chain_exhausted_low_quality` fell back to the retained answer. Every scored candidate is now retained with its score and exhaustion returns the best; and the last provider is scored too — judged by the PREVIOUS provider, so P2-11's never-self-judge rule holds — but ONLY when an earlier candidate exists to compare against, so the clean path costs no extra call. `_passes_quality` is renamed `_score_quality` and returns the float, an honest rename with no alias, matching the `llm_calls`→`llm_node_calls` precedent. | `evaluation/quality.py`, `llm/client.py`, `llm/router.py`, `config.py` |
| **D-61** | **Repeated evidence is fabricated consensus, and document identity is content, not a title.** Two independent fixes found in the same p205.211 trace. **(a) `guardrails/dedup.py` collapses byte-identical evidence per goal before it enters a prompt.** Tiers 1–3 of the D-38 ladder all resolve to the SAME ingested documents, so every corpus hit reappears tagged `mcp` with identical content, and again on each gather lap. Counted directly from that run's compiler prompt (45,063 chars / 10,626 prompt tokens, for the query "Compare India and US"): one Redis sentence appeared **26 times**, a second 22, a third 13 — four sentences, 67 occurrences, in a report about two countries. The token cost is real but secondary; the correctness cost is that a model shown one sentence twenty-six times is being told by sheer repetition that this is the most corroborated fact in the block. This is the SAME argument `websearch/filtering.cap_by_domain` already won for web results (D-57c) — five hits from one site read as five independent sources agreeing — applied to the corpus/MCP path, which had no equivalent guard at a far higher multiple. Deliberately narrow: keyed on `(goal_id, content_id(content))` so a sentence genuinely covering two goals is kept for each (the compiler needs both tags to cite either); only BYTE-identical content collapses, since deciding two differently-worded sentences are the same claim is a semantic judgement this package's own rule keeps out; and it touches ONLY the list passed to `compile_report`/`critique`, never `ResearchState.evidence`, so `recall`, `grounded_score`, `corpus_recall`, `grounding_ratio` and `evidence_by_source` all keep counting what was genuinely retrieved. Applied to the critic prompt for a second reason: `templates.critique` caps its evidence block at the last 60 items, so 67 duplicates could crowd out the evidence a claim actually rests on and reintroduce exactly the blindness D-46 fixed. **(b) `rrf_fuse`'s join key is now `content_id(content)`, not `title or content[:60]`.** Closes a limitation this README carried for several revisions. Both halves of the old key were wrong in the same direction: two different documents sharing a title (or a 60-char boilerplate prefix) FUSED INTO ONE, so one vanished silently and the other inherited a rank it never earned; and the same document titled in one store but not the other NEVER fused, scoring a genuine two-leg agreement as two single-leg hits — which matters concretely, because a rank-0 single-leg hit squashes to exactly the `min_evidence_score` boundary (the P2-01 follow-up collision). `content_id` is the uuid5 identity this codebase already uses for Qdrant point ids and memory dedup, so "the same document" now means the same thing in retrieval as it does in storage, rather than a third notion of identity. Neither store chunks, so one JSONL line is one document in both and the contents are byte-identical by construction. | `guardrails/dedup.py`, `agents/compilation.py`, `retrieval/hybrid.py` |
| **D-62** | **`compose_goals` was the one prompt builder that inlined retrieved text with no fence.** `agents/planning.py::goal_manager_node` collects `[e.content[:150] for e in state.evidence if e.source == "memory"]` and `prompts/templates.py::compose_goals` rendered them as bare `- {h}` bullets — no `fence_untrusted`, no `<evidence>` wrapper — while `compile_report`, `critique`, `generate_gaps` and `detect_contradictions` all fenced theirs (D-18/D-51). Three things made this the worst instance rather than the least important. **It runs second in every run, before any goal exists,** and its output IS the goal set, so a hijack here steers the whole run rather than one section of one report. **Memory content is durable** — corpus evidence is re-retrieved each run and a poisoned document can be pulled back out, but a poisoned string that reaches Qdrant persists across every future run until someone notices. **And the hijack already happened here by accident:** D-42's own trace has an earlier army run's PLA prose recalled into an India-vs-US run and composing an entirely military goal set. D-42 fixed that with a prompt INSTRUCTION ("the background must not narrow or re-frame the question"), which stays exactly as it was — but D-18's argument, restated by D-51, is that an instruction the model is merely asked to follow needs a deterministic enforcement half beside it. The fence is that half; defence in depth, not a guarantee. Hints are fenced INDIVIDUALLY rather than as a joined block, matching `compile_report`/`critique`, so one hint containing a literal `</evidence>` cannot close the span early and escape it. `guidance` is deliberately left unfenced and OUTSIDE the span: it is typed by a human reviewer at an E1 escalation, i.e. from inside the trust boundary, and D-23 injects it verbatim on purpose. | `prompts/templates.py`, `tests/unit/test_prompts_memory_fencing.py` |

## Not yet decided / explicitly deferred

See `internal/PHASE-2_PLAN.md`'s "Explicitly not planned" table for the full,
reasoned list (dead-code-safe rewrite of `rrf_fuse`, `decay_factor` retention
as a parity oracle, no internal worker retry, no dynamic/LLM-decided control
flow, production concerns out of scope). Not duplicated here to avoid the two
lists drifting apart — that table is the source of truth for "won't do."